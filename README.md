# Сервис сбора тарифов Wildberries

Сервис автоматически получает коэффициенты доставки с API Wildberries и выгружает их в Google Таблицу, а также сохраняет в локальную PostgreSQL базу данных. Подходит для анализа динамики тарифов и автоматизации учёта логистических расходов.

---

## 🛠️ Используемые технологии

| Технология                  | Назначение |
|-----------------------------|----------|
| **TypeScript**              | Язык программирования с строгой типизацией |
| **Node.js (v22.20.0)**      | Среда выполнения серверного кода |
| **Docker & Docker Compose** | Контейнеризация приложения и базы данных |
| **PostgreSQL (v17)**        | Хранение исторических данных о тарифах |
| **Knex.js**                 | SQL-запрос-билдер и миграции базы данных |
| **Google Sheets API**       | Обновление данных в Google Таблице |
| **Axios**                   | HTTP-клиент для запросов к API Wildberries |
| **node-cron**               | Запуск задач по расписанию (например, каждый час) |
| **dotenv**                  | Управление конфигурацией через `.env` файл |

---

## 🚀 Как запустить проект


### 1. Создайте файл `.env` описанный в  .env.example

### 2. Подготовьте `google-credentials.json`

Для доступа к Google Таблицам нужно:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект (например, `wb-tariff-sync`)
3. Включите **Google Sheets API**
4. Перейдите: **IAM & Admin → Service Accounts**
5. Создайте сервисный аккаунт и скачайте JSON-ключ
6. Переименуйте файл в `google-credentials.json` и поместите в:

### 3. Запуск
bash docker-compose up --build

Сервис:
- Запустит PostgresSQL
- Применит миграции
- Каждый час (или по вашему расписанию) будет:
  - Получать тарифы с Wildberries
  - Сохранять в БД
  - Обновлять Google Таблицу

---

### 🧪 Для тестирования (каждую минуту)

Измените в `.env`:

env CRON_SCHEDULE=* * * * *


> ⚠️ После тестирования верните обратно: `0 * * * *`